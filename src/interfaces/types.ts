export type ImageApi = {
    id: string
    alt_description: string
    urls: {
        full: string
        small: string
        thumb: string
    }
}

export type Image = {
    id: string
    altDescription: string
    urls: {
        full: string
        small: string
        thumb: string
    }
}
